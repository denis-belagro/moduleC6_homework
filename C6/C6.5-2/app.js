const btn = document.querySelector('.j-btn-test');
const height = window.screen.height
const lWidth = window.screen.width


btn.addEventListener('click', () => {
  
  alert(` высота: ${height}, ширина: ${lWidth}`)

});